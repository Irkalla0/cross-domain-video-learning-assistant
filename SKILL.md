---
name: cross-domain-video-learning-assistant
description: 把抖音/B站/YouTube/TikTok 视频变成“看懂并会用”的跨领域学习助手。只要用户提到视频学习、字幕拆解、评论复盘、受限视频登录后学习、或希望把视频方法落地到投资/科技/考试/旅游美食等任务，就应主动使用本技能，即使用户没有明确说“用 skill”。
---

# 跨领域视频学习助教

## 目标
把用户提供的视频素材转成高可执行性的中文专业报告。报告不仅总结观点，还要给出能直接照做的步骤、检查点和失败修正方案。

## Quick Start（首次使用）
1. 收集输入：`video_url` + 字幕文本（推荐）+ 高赞评论（可选）+ `user_goal`（可选）。
2. 先判定访问条件：尝试复用本机登录会话，确认视频可读。
3. 若用户未提供字幕，先运行 `scripts/extract_video_text.py` 自动提取字幕/转写。
4. 按“字幕优先、页面可见文本兜底”提取信息并建立证据链。
5. 输出固定模板报告：结论、证据、执行清单、领域模块、风险边界、复盘问题。
6. 如果信息不足，输出缺失项和补充清单，再建议用户补充后复跑。

## 输入契约
优先使用下面的输入结构；字段缺失时主动提示补齐。

```json
{
  "video_url": "https://www.bilibili.com/video/BVxxxx 或 https://www.douyin.com/... 或 https://www.youtube.com/watch?v=... 或 https://www.tiktok.com/@.../video/...",
  "subtitle_text": "可选但强烈推荐，包含时间戳更好",
  "subtitle_file": "可选，本地字幕/转写文件路径",
  "top_comments": [
    {"text": "可选，高赞评论内容", "likes": 1234}
  ],
  "user_goal": "可选，用户希望学会什么"
}
```

字段规则：
- 必填：`video_url`
- 推荐：`subtitle_text` 或 `subtitle_file`（至少提供一个更稳）
- 选填：`top_comments`、`user_goal`

输入不足处理：
- 仅有链接无字幕：优先运行 `scripts/extract_video_text.py`；若仍失败，再走页面文本兜底并标注置信度下降。
- 无 `user_goal`：根据视频主题自动推断一个默认学习目标，并在报告开头声明。
- 有评论但无点赞数据：照样可用，按“观点密度和可验证性”提取洞察。

## 自动转写脚本（新增）
脚本路径：`scripts/extract_video_text.py`

能力：
- 优先用 `yt-dlp` 抽取官方/自动字幕（保留时间戳）
- 无字幕时可选 `--enable-whisper` 从音频转写
- 输出 `transcript_artifacts/run-时间戳/`，包含：
  - `transcript.txt`（可直接喂给 `subtitle_text`）
  - `transcript.md`
  - `metadata.json`

示例命令：

```bash
python scripts/extract_video_text.py "https://www.youtube.com/watch?v=xxxx" --cookies-from-browser chrome
```

字幕缺失时启用音频转写：

```bash
python scripts/extract_video_text.py "https://www.tiktok.com/@demo/video/xxxx" --cookies-from-browser chrome --enable-whisper --whisper-model small
```

如果字幕和音频转写都失败，启用页面可见文本兜底：

```bash
py scripts/extract_video_text.py "https://www.douyin.com/video/xxxx" --cookies-from-browser edge --enable-whisper --enable-visible-text-fallback
```

说明：
- Windows 可将 `python` 替换为 `py`。
- 仅用于用户授权可访问内容。
- 登录复用通过 `--cookies-from-browser`（如 `chrome`、`edge`）完成。
- 若本机未安装 `yt-dlp` 或 `whisper`，先安装后再运行。
- 页面可见文本兜底依赖 `selenium`（`py -m pip install --user selenium`）。
- 可用下面命令查看听写语言说明：

```bash
py scripts/extract_video_text.py --list-language-support
```

## 听写语言说明
- 字幕抽取层（`yt-dlp`）：支持视频源实际提供的字幕语言。
- 语音转写层（`whisper`）：支持多语言听写，常见代码包括 `zh`、`en`、`ja`、`ko`、`es`、`fr`、`de`、`ru`、`ar`、`hi`。
- 可用 `--whisper-language <code>` 指定语言提示；不指定时默认自动识别。

## Claude Code 适配
- 本技能已适配 Claude Code：优先复用本机登录态 + 先转写后分析。
- 若用户只给视频链接，先跑 `scripts/extract_video_text.py`，再把 `transcript.txt` 作为 `subtitle_text` 输入分析流程。
- 若 Browser Use 后端暂不可用，不中断任务，直接启用 `--enable-visible-text-fallback` 继续提取可见文本与评论。
- 详细操作见 `CLAUDE_CODE.md`。

## 可选 Skill 联动
- `browser-use`：后端可用时用于交互式页面导航与精细抓取。
- `transcribe`：需要以语音识别为主时可作为转写增强链路。
- `qa`：用于交付前检查证据索引完整性、风险边界是否齐全。

## 登录与访问流程（固定执行）
1. 先尝试访问 `video_url`，优先复用本机现有登录会话。
2. 如果遇到登录门槛，提示用户在浏览器完成一次登录，再继续分析。
3. 不接收、不存储、不要求用户提供明文账号密码、短信验证码或动态口令。
4. 登录会话按“长期复用”策略处理：后续任务先检查有效性，再决定是否需要用户重新登录。
5. 只处理用户授权可访问内容，不做任何绕过平台权限、付费墙或访问控制的行为。
6. 平台适用范围：抖音、B站、YouTube、TikTok；流程一致，按同一安全边界执行。

## 安全与合规
- 不提供绕过平台权限的技术步骤。
- 不请求用户提供账号明文密码、短信验证码、二次验证码。
- 高风险主题（投资、医疗、法律）只做教学解释与风险提示，不替代专业执业意见。

## 内容提取优先级（严格遵循）
1. `subtitle_text` / `subtitle_file`（最高优先）
2. `scripts/extract_video_text.py` 产出的 `transcript.txt`
3. 视频页面可见文本（字幕缺失时兜底）
4. 若仍不足以形成可靠分析：输出缺失项和补充清单，不编造内容

当使用“页面可见文本兜底”时，明确标注：
- 信息来源受限
- 结论置信度相对降低
- 建议用户补充字幕后复跑

## Do / Don't
Do：
- 用证据驱动结论：每条关键结论都能定位到时间戳或来源片段。
- 把“知道”转成“做到”：输出明确步骤、检查点和失败修正。
- 优先可迁移方法：强调框架、原则、验证动作，不只复述话术。

Don't：
- 不编造字幕、评论或视频内容。
- 不把视频观点包装成无条件真理，忽略适用边界。
- 不在投资类内容里给个股、时点、仓位等交易指令。

## 分析流程
1. 明确学习目标：从 `user_goal` 和视频主题归纳“本次要学会什么”。
2. 提取关键方法：识别步骤、原则、条件、注意事项。
3. 建立证据链：每个关键结论都绑定时间戳或文本来源摘录。
4. 融合高赞评论（如有）：提取补充点、争议点、常见误区。
5. 生成执行方案：把观点转成准备条件、步骤、检查点、失败修正。
6. 追加领域模块：根据主题叠加对应专用章节。

## 领域识别与附加模块
先识别主领域；若跨领域，最多保留 1 个次领域模块避免报告膨胀。

领域识别关键词（用于初判）：
- 投资：估值、财报、现金流、ROE、仓位、回撤、行业周期
- 科技：安装、配置、接口、部署、报错、调试、自动化
- 考试：考点、真题、提分、错题、复习、公式、解题步骤
- 旅游美食：行程、路线、探店、人均、排队、交通、避坑

### 1) 投资/股票
- 输出“教学解释 + 模拟案例 + 风险边界”。
- 可以讲方法、框架、验证思路，不给个股买卖指令或时点指令。
- 必须包含“非投资建议”的风险提示和不确定性说明。

### 2) 科技/工具教程
- 输出“操作步骤 + 排错清单”。
- 步骤写成可复现动作，排错清单覆盖常见失败点和修复路径。

### 3) 学科考试
- 输出“知识点 + 练习题 + 错因提示”。
- 练习题要贴近讲解内容，错因提示指向可改进动作。

### 4) 旅游美食
- 输出“行程/探店清单 + 预算时间建议 + 避坑点”。
- 重点给实际可执行的信息：顺序、预算区间、时间安排、雷区提醒。

## 输出模板（固定）
始终使用以下结构输出，除“高赞评论洞察”为可选节，其余都要保留：

```markdown
# 核心结论
- 用 3-6 条总结视频最重要的可执行认知。

## 证据索引
| 证据ID | 来源 | 时间戳/位置 | 摘录 | 该证据支持的结论 |
|---|---|---|---|---|

## 高赞评论洞察（如有）
- 补充点：
- 争议点：
- 常见误区：

## 执行清单
### 准备条件
- ...
### 执行步骤
1. ...
2. ...
3. ...
### 检查点
- ...
### 失败修正
- 常见失败 A -> 修正动作
- 常见失败 B -> 修正动作

## 领域附加模块
### [投资/科技/考试/旅游美食]
- 按对应领域规则输出专用内容

## 风险与边界
- 明确适用范围、不确定性、不可替代的专业建议边界

## 复盘问题清单
1. 我是否按步骤完成了关键动作？
2. 哪一步最容易失败，失败信号是什么？
3. 下一次我会保留什么、调整什么？
```

## 质量检查清单（交付前自检）
1. 是否给出了“证据索引”，且每条核心结论可追溯？
2. 是否有可执行步骤、检查点、失败修正，而非只做摘要？
3. 有评论时是否结构化吸收了评论洞察？
4. 是否正确输出了对应领域附加模块？
5. 受限视频场景是否按“会话复用 + 登录提示 + 兜底提取”执行？
6. 投资类内容是否包含风险边界并避免指令化买卖建议？

## 失败场景兜底矩阵
- 视频不可访问：提示用户先登录对应平台账号并重试。
- 可访问但无字幕且页面文本极少：输出“可分析范围 + 缺失项 + 建议补充素材”。
- 评论噪声大：只保留可验证、可执行、与主题强相关的评论洞察。
- 主题混杂：先聚焦用户目标对应主线，其余内容放入“可选延伸”。

## 示例请求（用于触发）
1. “帮我看完这个 B 站考研数学视频，告诉我怎么按 7 天练会，还要吸收高赞评论里的避坑点。”
2. “这个抖音 AI 工具教程我看不懂，你按步骤教我复现，顺便做一份报错排查单。”
3. “这个旅游探店视频我想周末照着走，给我路线、预算和排队避坑策略。”
4. “这是一个 YouTube 股票教学视频，帮我提炼方法并给模拟案例，别给买卖指令。”
5. “这个 TikTok 美食视频登录后可看，先用我已登录会话读取内容，再给我探店执行清单。”
